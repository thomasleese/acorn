import 'base'

type Vector2
    x as Integer
    y as Integer
end

def length2(v as Vector2) as Integer
    return (v.x * v.x) + (v.y * v.y)
end

let v = Vector2(x: 10, y: 10)
let l = v.length2()

exit(code: l)
