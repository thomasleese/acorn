import 'env'
import 'io'


let args = args()

if len(args) <= 2:
    print(format("Usage: {} <filename>", args[0]))
    return

let file = open(args[1])
let contents = read(file)
close(file)

print(contents)
