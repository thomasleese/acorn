type UserError as enum:
    NotFound
    Banned(Date)

type User as object:
    id as Int

func find_user(name as Str) as User:
    throw UserError.NotFound()

try:
    let user = find_user("Thomas")
except UserError.NotFound:
    pass
except UserError.Banned(let since):
    pass
