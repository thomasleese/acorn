type builtin Int64
type builtin Void

type Int as Int64

def exit(code as Int)
  ccall exit(Int) as Void using code
end

type Vector2
  x as Int
  y as Int
end

let v = Vector2.new(y: 10, x: 0)

exit(v.x)
