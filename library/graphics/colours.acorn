type Colour{T}
    r as T
    g as T
    b as T
    a as T
end

type Color{T} as Colour{T}  # to please those Americans :)

def to_html_string(colour as Colour{Integer8}) as String
    return "#" + hex(n: colour.r) + hex(n: colour.g) + hex(n: colour.b)
end
