type Colour
    r as Integer8
    g as Integer8
    b as Integer8
    a as Integer8
end

type Color as Colour  # to please those Americans :)

def to_html_string(colour as Colour) as String
    return "#" + hex(n: colour.r) + hex(n: colour.g) + hex(n: colour.b)
end
