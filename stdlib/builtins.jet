type Enumerate
    iterator as T
end

def enumerate(iterator as T) as Enumerate
    Enumerate(iterator: iterator)
end

def length(e as Enumerate) as Integer
    length(e: e.iterator)
end

def start(e as Enumerate) as Tuple
    #(0, start(e.iterator))
end

def next(e as Enumerate, state as Tuple) as Tuple
    #n = next(e.itr, state[1])
    #(state[0], n[0]), (state[0] + 1, n[1])
end

def done(e as Enumerate, state as Tuple) as Void
    #done(e.iterator, state[1])
end
