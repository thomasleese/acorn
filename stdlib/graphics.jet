type Colour
    r as Byte
    g as Byte
    b as Byte
    a as Byte
end

let Color as Type = Colour

def to_html_string(colour as Colour) as String
    "#" + hex(str: colour.r) + hex(str: colour.g) + hex(str: colour.b)
end
