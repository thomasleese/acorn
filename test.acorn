type Integer as Integer64

enum MaybeInteger
    Some as Integer
    Nothing
end

let perhaps_integer = MaybeInteger.Some(10)

match perhaps_integer
    where MaybeInteger.Some(let n)
        exit(n)
    end

    where MaybeInteger.Nothing
        exit(25)
    end
end
