import 'base'

protocol Iterator{T, E, S}
    def start(T) as S
    def next(T, S) as Tuple{E, S}
    def done(T, S) as Boolean
end

let a = create_array(Integer, 15)

let i = 0
for i in range(10)
    a.append(i)
end

exit(a.length)
