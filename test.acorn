import 'base'

def sum(iterator as Array{Integer}) as Integer
    let total = 0

    # iterator over values
    let i = 0
    for i in iterator
        total += i
    end

    return total
end

let a = create_array(Integer, 15)

let i = 0
for i in range(10)
    a.append(i)
end

exit(sum(a))
