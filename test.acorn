import 'base'

let a = create_array(Integer, 20)

let i = 0
while i < 6 do
    a.append(i)
    i += 1
end

let sum = 0

for i in a do
    sum += i
end

let state = start(a)
while not done(a, state) do
    let temp = next(a, state)
    state = temp.1
    sum += temp.0
end

exit(sum)
